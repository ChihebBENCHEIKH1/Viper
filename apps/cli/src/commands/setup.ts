/**
 * Setup command — interactive credential configuration wizard (npx mode only).
 */

export async function setup(): Promise<void> {
  // TODO: Implement interactive TUI wizard using @clack/prompts
  // 1. Ask for Anthropic API key
  // 2. Validate key format
  // 3. Write to ~/.viper/config.toml
  console.log('Setup wizard not yet implemented.');
  console.log('For now, set ANTHROPIC_API_KEY as an environment variable.');
}
